import { useState, useEffect } from \"react\"\nimport BrainPortal from \"../components/BrainPortal\"\n\nexport default function Splash({ mousePos, onComplete }) {\n  const [stage, setStage] = useState(\"initial\") // initial, exploding, text, dissolving, done\n  const [explosionProgress, setExplosionProgress] = useState(0)\n\n  // Trigger the brain click-explosion sequence\n  const handleExplode = () => {\n    if (stage !== \"initial\") return\n    setStage(\"exploding\")\n\n    // Animate the R3F fragments over 1.5 seconds\n    const startTime = Date.now()\n    const duration = 1500\n    const interval = setInterval(() => {\n      const elapsed = Date.now() - startTime\n      const progress = Math.min(elapsed / duration, 1)\n      setExplosionProgress(progress)\n      \n      if (progress >= 1) {\n        clearInterval(interval)\n        // Transition to the golden typewriter text\n        setStage(\"text\")\n        playTextStage()\n      }\n    }, 16)\n  }\n\n  // Manage typewriter thought display duration and pollen dissolve transition\n  const playTextStage = () => {\n    // Show text for 3 seconds\n    setTimeout(() => {\n      setStage(\"dissolving\")\n      // Dissolve text over 1.5 seconds, then enter app\n      setTimeout(() => {\n        setStage(\"done\")\n        onComplete()\n      }, 1500)\n    }, 3000)\n  }\n\n  const thoughtText = \"Every morning is a fresh page. Start writing. 🌱\"\n  const thoughtWords = thoughtText.split(\" \")\n\n  return (\n    <div style={{\n      position: \"fixed\",\n      inset: 0,\n      zIndex: 9999,\n      display: \"flex\",\n      flexDirection: \"column\",\n      alignItems: \"center\",\n      justifyContent: \"center\",\n      background: \"transparent\",\n      overflow: \"hidden\"\n    }}>\n      <style>{`\n        /* Typewriter text styling in gold */\n        .typewriter-thought {\n          font-family: 'Special Elite', cursive;\n          font-size: 28px;\n          font-weight: 400;\n          color: #FFD700;\n          text-align: center;\n          line
<truncated 3995 bytes>