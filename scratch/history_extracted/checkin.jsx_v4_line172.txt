import { useState, useRef, useEffect } from \"react\"\nimport { motion, AnimatePresence } from \"framer-motion\"\nimport axios from \"axios\"\nimport Lumie from \"../components/Lumie\"\n\nconst API = \"http://localhost:8000\"\n\nconst ORBS_CONFIG = [\n  { icon: \"🌑\", label: \"seed\" },\n  { icon: \"🌱\", label: \"sprout\" },\n  { icon: \"🌸\", label: \"blossom\" },\n  { icon: \"🌳\", label: \"full tree\" }\n]\n\n// Blinking Typewriter Question Component\nfunction TypewriterBubble({ text, speed = 22, onComplete }) {\n  const [displayedText, setDisplayedText] = useState(\"\")\n\n  useEffect(() => {\n    let index = 0\n    setDisplayedText(\"\")\n    const interval = setInterval(() => {\n      if (index < text.length) {\n        setDisplayedText(text.slice(0, index + 1))\n        index++\n      } else {\n        clearInterval(interval)\n        if (onComplete) onComplete()\n      }\n    }, speed)\n    return () => clearInterval(interval)\n  }, [text, speed])\n\n  return (\n    <span>\n      {displayedText}\n      <span className=\"typewriter-cursor\">|</span>\n    </span>\n  )\n}\n\n// Falling Petals Confetti Component\nfunction PetalConfetti() {\n  const [petals, setPetals] = useState([])\n\n  useEffect(() => {\n    const items = Array.from({ length: 60 }, (_, i) => ({\n      id: i,\n      left: Math.random() * 100,\n      size: Math.random() * 14 + 10,\n      delay: Math.random() * 3,\n      duration: Math.random() * 4 + 4,\n      drift: (Math.random() - 0.5) * 60\n    }))\n    setPetals(items)\n  }, [])\n\n  return (\n    <div style={{ position: \"fixed\", inset: 0, pointerEvents: \"none\", overflow: \"hidden\", zIndex: 1200 }}>\n      {petals.map(p => (\n        <div\n          key={p.id}\n          style={{\n            position: \"absolute\",\n            top: \"-20px\",\n            left: `${p.left}%`,\n            width: `${p.size}px`,\n            height: `${p.size * 0.7}px`,\n            background: \"radial-gradient(circle, #FFB7C5 0%, #FF8C42 100%)\",\n            borderRadius: \"50% 0 50% 0\"
<truncated 30724 bytes>