import { useState, useEffect, useRef } from \"react\"\nimport { motion, AnimatePresence } from \"framer-motion\"\n\nexport default function CustomCursor() {\n  const [position, setPosition] = useState({ x: -100, y: -100 })\n  const [isHovered, setIsHovered] = useState(false)\n  const [sparkles, setSparkles] = useState([])\n  const lastSparkleTime = useRef(0)\n\n  useEffect(() => {\n    // 1. Mouse move tracker\n    const handleMouseMove = (e) => {\n      const { clientX, clientY } = e\n      setPosition({ x: clientX, y: clientY })\n\n      // Rate limit sparkle spawning to avoid rendering overflow\n      const now = Date.now()\n      if (now - lastSparkleTime.current > 60) {\n        spawnSparkle(clientX, clientY)\n        lastSparkleTime.current = now\n      }\n    }\n\n    // 2. Clickable elements detector\n    const handleMouseOver = (e) => {\n      const target = e.target\n      if (!target) return\n      const isClickable = target.closest(\"a, button, [role='button'], .btn-press, [onClick]\")\n      setIsHovered(!!isClickable)\n    }\n\n    window.addEventListener(\"mousemove\", handleMouseMove)\n    window.addEventListener(\"mouseover\", handleMouseOver)\n\n    return () => {\n      window.removeEventListener(\"mousemove\", handleMouseMove)\n      window.removeEventListener(\"mouseover\", handleMouseOver)\n    }\n  }, [])\n\n  // Spawn trail sparkles\n  const spawnSparkle = (x, y) => {\n    const newSparkle = {\n      id: Date.now() + Math.random(),\n      x: x + (Math.random() - 0.5) * 10,\n      y: y + (Math.random() - 0.5) * 10,\n      size: Math.random() * 5 + 3\n    }\n    setSparkles(prev => [...prev.slice(-15), newSparkle]) // keep max 15 sparkles active\n  }\n\n  // Remove sparkle after animation completes\n  const removeSparkle = (id) => {\n    setSparkles(prev => prev.filter(s => s.id !== id))\n  }\n\n  return (\n    <div style={{\n      position: \"fixed\",\n      inset: 0,\n      zIndex: 99999,\n      pointerEvents: \"none\",\n      overflow: \"hidden\"\n    }}>\n      <style>{`\n        .firef
<truncated 1437 bytes>