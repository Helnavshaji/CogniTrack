import { useRef, useMemo } from \"react\"\nimport { Canvas, useFrame } from \"@react-three/fiber\"\nimport { Sphere, Cylinder, Torus, MeshDistortMaterial, Float, Sparkles } from \"@react-three/drei\"\nimport * as THREE from \"three\"\n\n// 3D Multi-part Brain component\nfunction Brain({ mouse }) {\n  const groupRef = useRef()\n  const ring1Ref = useRef()\n  const ring2Ref = useRef()\n\n  // Track cursor position to tilt/rotate the entire brain group in real-time\n  useFrame((state) => {\n    if (groupRef.current) {\n      // Full 3D perspective mouse tracking - rotates to show left/right sides\n      const targetY = (mouse.x * Math.PI) / 3 // Left/right rotation\n      const targetX = (-mouse.y * Math.PI) / 4 // Up/down rotation\n      \n      // Smooth lerp (0.05 interpolation factor)\n      groupRef.current.rotation.y += (targetY - groupRef.current.rotation.y) * 0.05\n      groupRef.current.rotation.x += (targetX - groupRef.current.rotation.x) * 0.05\n    }\n\n    // Slowly spin the halo rings at different angles and speeds\n    if (ring1Ref.current) {\n      ring1Ref.current.rotation.z += 0.006\n    }\n    if (ring2Ref.current) {\n      ring2Ref.current.rotation.y -= 0.008\n      ring2Ref.current.rotation.z += 0.004\n    }\n  })\n\n  return (\n    <group ref={groupRef}>\n      {/* 300 Golden Sparkles like synapses */}\n      <Sparkles count={300} scale={4.5} size={3} speed={0.5} color=\"#FFD700\" />\n\n      {/* Orbiting Halo 1 (Gold) */}\n      <mesh ref={ring1Ref} rotation={[Math.PI / 3, 0, 0]}>\n        <Torus args={[2.4, 0.035, 8, 64]}>\n          <meshBasicMaterial color=\"#FFD700\" transparent opacity={0.7} />\n        </Torus>\n      </mesh>\n\n      {/* Orbiting Halo 2 (Pink-White) */}\n      <mesh ref={ring2Ref} rotation={[-Math.PI / 4, Math.PI / 6, 0]}>\n        <Torus args={[2.7, 0.025, 8, 64]}>\n          <meshBasicMaterial color=\"#FFB7C5\" transparent opacity={0.6} />\n        </Torus>\n      </mesh>\n\n      {/* Float animation wrapper to give gentle floating up/down motion */}\n      <Float sp
<truncated 5986 bytes>