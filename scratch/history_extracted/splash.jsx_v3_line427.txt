import { useState, useEffect } from \"react\"\nimport BrainPortal from \"../components/BrainPortal\"\n\nexport default function Splash({ mousePos, onComplete, onZoom }) {\n  const [stage, setStage] = useState(\"initial\") // initial, exploding, text, dissolving, done\n  const [explosionProgress, setExplosionProgress] = useState(0)\n\n  // Trigger the brain click-explosion sequence\n  const handleExplode = () => {\n    if (stage !== \"initial\") return\n    setStage(\"exploding\")\n\n    // Act 1 — Shatter: brain breaks into 200 fragment pieces flying outward. Duration 800ms.\n    const startTime = Date.now()\n    const duration = 800\n    const interval = setInterval(() => {\n      const elapsed = Date.now() - startTime\n      const progress = Math.min(elapsed / duration, 1)\n      setExplosionProgress(progress)\n      \n      if (progress >= 1) {\n        clearInterval(interval)\n        // Act 2 — Thought reveal\n        setStage(\"text\")\n        playTextStage()\n      }\n    }, 16)\n  }\n\n  const playTextStage = () => {\n    // Reveal text in Special Elite for 2000ms\n    setTimeout(() => {\n      setStage(\"dissolving\")\n      if (onZoom) onZoom() // Zoom the forest background as a portal effect\n      \n      // Act 3 — Dissolve and transitions into forest\n      setTimeout(() => {\n        setStage(\"done\")\n        onComplete()\n      }, 1500)\n    }, 2000)\n  }\n\n  const thoughtText = \"Every sunrise is your mind asking for a fresh start 🌱\"\n  const thoughtWords = thoughtText.split(\" \")\n\n  return (\n    <div style={{\n      position: \"fixed\",\n      inset: 0,\n      zIndex: 9999,\n      display: \"flex\",\n      flexDirection: \"column\",\n      alignItems: \"center\",\n      justifyContent: \"center\",\n      background: \"transparent\",\n      overflow: \"hidden\",\n      pointerEvents: \"none\"\n    }}>\n      <style>{`\n        /* Special Elite Font styled thought */\n        .typewriter-thought {\n          font-family: 'Special Elite', cursive;\n          font-size: 28px;\n   
<truncated 4219 bytes>