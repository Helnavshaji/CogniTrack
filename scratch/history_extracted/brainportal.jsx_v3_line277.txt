import { useRef, useMemo } from \"react\"\nimport { Canvas, useFrame } from \"@react-three/fiber\"\nimport { Sphere, MeshDistortMaterial, Float, Sparkles } from \"@react-three/drei\"\nimport * as THREE from \"three\"\n\n// 3D Distorted Brain component\nfunction Brain({ mouse }) {\n  const meshRef = useRef()\n\n  // Track cursor position to tilt/rotate the brain inside Canvas context\n  useFrame(() => {\n    if (meshRef.current) {\n      const targetY = (mouse.x * Math.PI) / 4\n      const targetX = (-mouse.y * Math.PI) / 6\n      meshRef.current.rotation.y += (targetY - meshRef.current.rotation.y) * 0.05\n      meshRef.current.rotation.x += (targetX - meshRef.current.rotation.x) * 0.05\n    }\n  })\n\n  return (\n    <Float speed={2.5} rotationIntensity={0.4} floatIntensity={0.65}>\n      <mesh ref={meshRef}>\n        {/* Main brain shape — distorted sphere */}\n        <Sphere args={[1.8, 128, 128]}>\n          <MeshDistortMaterial\n            color=\"#FF6B9D\"\n            emissive=\"#FF3CAC\"\n            emissiveIntensity={0.4}\n            distort={0.4}\n            speed={2}\n            transparent\n            opacity={0.85}\n            metalness={0.2}\n            roughness={0.1}\n          />\n        </Sphere>\n        {/* Neural glow sparkles around brain */}\n        <Sparkles count={200} scale={5} size={2} speed={0.4} color=\"#FFD700\" />\n      </mesh>\n    </Float>\n  )\n}\n\n// Fragment Explosion component (GPU-based instancedMesh)\nfunction BrainExplosion({ explosionProgress, particles }) {\n  const meshRef = useRef()\n  const tempObject = useMemo(() => new THREE.Object3D(), [])\n\n  useFrame(() => {\n    if (!meshRef.current) return\n    const t = explosionProgress // scales from 0 to 1\n    const sizeScale = Math.max(0, 1 - t) // shrink fragments over time\n\n    for (let i = 0; i < 200; i++) {\n      const p = particles[i]\n      \n      // Calculate linear outward movement with velocity\n      const x = p.pos[0] + p.vel[0] * t * 3.5\n      const y = p.pos[1] + p.vel[1] * t * 3.5\n      
<truncated 2591 bytes>