import { useRef, useMemo, useEffect } from \"react\"\nimport { Canvas, useFrame } from \"@react-three/fiber\"\nimport { Points, PointMaterial } from \"@react-three/drei\"\nimport * as THREE from \"three\"\n\n// Subcomponent to animate the brain particles inside the Canvas context\nfunction BrainMesh({ mousePos, isExploding, explosionProgress }) {\n  const pointsRef = useRef()\n  const linesRef = useRef()\n  \n  const particleCount = 750\n\n  // Procedurally generate brain lobes\n  const [positions, velocities, colors, originalPositions] = useMemo(() => {\n    const pos = new Float32Array(particleCount * 3)\n    const origPos = new Float32Array(particleCount * 3)\n    const vels = new Float32Array(particleCount * 3)\n    const cols = new Float32Array(particleCount * 3)\n\n    for (let i = 0; i < particleCount; i++) {\n      const isLeft = Math.random() > 0.5\n      // Ellipsoidal coordinate generator with sines for folds\n      const theta = Math.random() * Math.PI\n      const phi = Math.random() * Math.PI * 2\n\n      // Brain folds sines formula\n      const folds = 1.3 + Math.sin(theta * 10) * Math.sin(phi * 8) * 0.12\n      let x = folds * Math.sin(theta) * Math.cos(phi) * 0.9\n      const y = folds * Math.sin(theta) * Math.sin(phi) * 0.75\n      const z = folds * Math.cos(theta) * 0.75\n\n      // Separate hemispheres\n      if (isLeft) {\n        x -= 0.15\n      } else {\n        x += 0.15\n      }\n\n      // Initial positions\n      pos[i * 3] = x\n      pos[i * 3 + 1] = y\n      pos[i * 3 + 2] = z\n\n      origPos[i * 3] = x\n      origPos[i * 3 + 1] = y\n      origPos[i * 3 + 2] = z\n\n      // Velocity vectors for explosion scattering\n      const angle = Math.random() * Math.PI * 2\n      const speed = Math.random() * 4 + 2\n      vels[i * 3] = x * speed + (Math.random() - 0.5) * 2\n      vels[i * 3 + 1] = y * speed + (Math.random() - 0.5) * 2\n      vels[i * 3 + 2] = z * speed + (Math.random() - 0.5) * 2\n\n      // Color mapping: gold (#FFD700) or dawn pink (#FFB7C5)\n      const isGold = Math.ran
<truncated 5369 bytes>