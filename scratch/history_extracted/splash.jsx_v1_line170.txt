import { useState, useEffect } from \"react\"\nimport { motion, AnimatePresence } from \"framer-motion\"\nimport BrainPortal from \"../components/BrainPortal\"\n\nexport default function Splash({ mousePos, onComplete }) {\n  const [stage, setStage] = useState(\"initial\") // initial, exploding, text, dissolving, done\n  const [explosionProgress, setExplosionProgress] = useState(0)\n\n  // Trigger the click-explosion sequence\n  const handleExplode = () => {\n    if (stage !== \"initial\") return\n    setStage(\"exploding\")\n\n    // Animate the Three.js brain points scattering over 1.5 seconds\n    let startTime = Date.now()\n    const duration = 1500\n    const interval = setInterval(() => {\n      const elapsed = Date.now() - startTime\n      const progress = Math.min(elapsed / duration, 1)\n      setExplosionProgress(progress)\n      if (progress >= 1) {\n        clearInterval(interval)\n        // Transition to the golden typewriter text stage\n        setStage(\"text\")\n        playTextStage()\n      }\n    }, 16)\n  }\n\n  // Manage typewriter script display and pollen dissolve transition\n  const playTextStage = () => {\n    setTimeout(() => {\n      setStage(\"dissolving\")\n      // Wait for words to float up and dissolve, then enter main app\n      setTimeout(() => {\n        setStage(\"done\")\n        onComplete()\n      }, 1500)\n    }, 3800) // Float typewriter text for ~3.8 seconds\n  }\n\n  const thoughtText = \"Every morning is a chance to grow something beautiful 🌱\"\n  const thoughtWords = thoughtText.split(\" \")\n\n  return (\n    <div style={{\n      position: \"fixed\",\n      inset: 0,\n      zIndex: 9999,\n      display: \"flex\",\n      flexDirection: \"column\",\n      alignItems: \"center\",\n      justifyContent: \"center\",\n      overflow: \"hidden\",\n      fontFamily: \"'DM Sans', sans-serif\"\n    }}>\n      <style>{`\n        /* Typewriter font & styling */\n        .typewriter-thought {\n          font-family: 'Special Elite', cursive;\n          font-size: 28px;\n       
<truncated 5479 bytes>