{
  "target": "function MainLayout({ userId }) {\\n  const location = useLocation()\\n  const [sideOpen, setSideOpen] = useState(false)\\n  const [splashCompleted, setSplashCompleted] = useState(() => {\\n    return !!sessionStorage.getItem('splash_shown')\\n  })\\n  const [mousePos, setMousePos] = useState({ x: 0, y: 0 })\\n\\n  useEffect(() => {\\n    // Track normalized mouse coordinates (-1 to 1) for parallax\\n    const handleMouseMove = (e) => {\\n      const x = (e.clientX - window.innerWidth / 2) / (window.innerWidth / 2)\\n      const y = (e.clientY - window.innerHeight / 2) / (window.innerHeight / 2)\\n      setMousePos({ x, y })\\n    }\\n    window.addEventListener(\\\"mousemove\\\", handleMouseMove)\\n    return () => window.removeEventListener(\\\"mousemove\\\", handleMouseMove)\\n  }, [])\\n\\n  const isCheckinPage = location.pathname === '/checkin'\\n\\n  return (\\n    <div style={{ position: 'relative', minHeight: '100vh', width: '100%', background: 'transparent' }}>\\n      {/* Global Custom Cursor */}\\n      <CustomCursor />\\n\\n      {/* Persistent Forest Background Scene */}\\n      <ForestScene mousePos={mousePos} />\\n\\n      {/* 3D Brain Splash Overlay Page */}\\n      {!splashCompleted && (\\n        <Splash \\n          mousePos={mousePos} \\n          onComplete={() => {\\n            setSplashCompleted(true)\\n            sessionStorage.setItem('splash_shown', 'true')\\n          }} \\n        />\\n      )}\\n\\n      {/* Main Content (revealed after splash completed) */}\\n      {splashCompleted && (\\n        <div style={{ background: 'transparent', animation: 'slideInUp 1.0s cubic-bezier(0.16, 1, 0.3, 1) both' }}>\\n          {/* Floating Glassmorphism Navbar */}\\n          <FloatingNav />\\n\\n          {/* Render global Lumie on non-checkin pages */}\\n          {!isCheckinPage && <Lumie checkinState=\\\"idle\\\" tone=\\\"\\\" />}\\n\\n          {/* Slide-out Side Dashboard drawer */}\\n          <SideDashboard \\n            userId={userId} \\n            isOpen={sideOpen} \\n            onToggle={() => setSideOpen(!sideOpen)} \\n          />\\n\\n          {\n<truncated 740 bytes>",
  "replacement": "function MainLayout({ userId }) {\\n  const location = useLocation()\\n  const [sideOpen, setSideOpen] = useState(false)\\n  const [splashCompleted, setSplashCompleted] = useState(false) // Show 3D brain splash on every load/reload\\n  const [mousePos, setMousePos] = useState({ x: 0, y: 0 })\\n  const [activeCheckinState, setActiveCheckinState] = useState(\\\"idle\\\")\\n  const [activeCheckinTone, setActiveCheckinTone] = useState(\\\"\\\")\\n\\n  useEffect(() => {\\n    // Track normalized mouse coordinates (-1 to 1) for parallax\\n    const handleMouseMove = (e) => {\\n      const x = (e.clientX - window.innerWidth / 2) / (window.innerWidth / 2)\\n      const y = (e.clientY - window.innerHeight / 2) / (window.innerHeight / 2)\\n      setMousePos({ x, y })\\n    }\\n    window.addEventListener(\\\"mousemove\\\", handleMouseMove)\\n    return () => window.removeEventListener(\\\"mousemove\\\", handleMouseMove)\\n  }, [])\\n\\n  // Reset active state when leaving checkin route\\n  useEffect(() => {\\n    if (location.pathname !== '/checkin') {\\n      setActiveCheckinState(\\\"idle\\\")\\n      setActiveCheckinTone(\\\"\\\")\\n    }\\n  }, [location.pathname])\\n\\n  return (\\n    <div style={{ position: 'relative', minHeight: '100vh', width: '100%', background: 'transparent' }}>\\n      {/* Global Custom Cursor */}\\n      <CustomCursor />\\n\\n      {/* Persistent Forest Background Scene */}\\n      <ForestScene mousePos={mousePos} />\\n\\n      {/* 3D Brain Splash Overlay Page */}\\n      {!splashCompleted && (\\n        <Splash \\n          mousePos={mousePos} \\n          onComplete={() => {\\n            setSplashCompleted(true)\\n          }} \\n        />\\n      )}\\n\\n      {/* Main Content (revealed after splash completed) */}\\n      {splashCompleted && (\\n        <div style={{ background: 'transparent', animation: 'slideInUp 1.0s cubic-bezier(0.16, 1, 0.3, 1) both' }}>\\n          {/* Floating Glassmorphism Navbar */}\\n          <FloatingNav />\\n\\n          {/* Render unified global Lumie companion */}\\n          <Lumie checkinState={activeCheckinState} tone={activeCh\n<truncated 1094 bytes>"
}