import { useState, useEffect, useRef } from \"react\"\nimport { motion, AnimatePresence } from \"framer-motion\"\n\nexport default function CustomCursor() {\n  const [position, setPosition] = useState({ x: -100, y: -100 })\n  const [isHovered, setIsHovered] = useState(false)\n  const [trail, setTrail] = useState([]) // Store last 12 positions for trailing dots\n  const requestRef = useRef()\n  const mousePosRef = useRef({ x: -100, y: -100 })\n  const currentPosRef = useRef({ x: -100, y: -100 })\n\n  useEffect(() => {\n    // 1. Mouse move tracker\n    const handleMouseMove = (e) => {\n      mousePosRef.current = { x: e.clientX, y: e.clientY }\n    }\n\n    // 2. Clickable elements detector\n    const handleMouseOver = (e) => {\n      const target = e.target\n      if (!target) return\n      const isClickable = target.closest(\"a, button, [role='button'], .btn-press, [onClick], input, textarea, select\")\n      setIsHovered(!!isClickable)\n    }\n\n    window.addEventListener(\"mousemove\", handleMouseMove)\n    window.addEventListener(\"mouseover\", handleMouseOver)\n\n    // 3. Smooth animation loop (150ms spring-like lag interpolation)\n    const animate = () => {\n      // Lerp (Linear Interpolation) with a factor to simulate 150ms delay/floaty feel\n      const dx = mousePosRef.current.x - currentPosRef.current.x\n      const dy = mousePosRef.current.y - currentPosRef.current.y\n      \n      // Update main firefly position (floaty lerp)\n      currentPosRef.current.x += dx * 0.15\n      currentPosRef.current.y += dy * 0.15\n\n      setPosition({ x: currentPosRef.current.x, y: currentPosRef.current.y })\n\n      // Store trail history\n      setTrail(prev => {\n        const next = [{ x: currentPosRef.current.x, y: currentPosRef.current.y }, ...prev]\n        return next.slice(0, 15) // Keep last 15 ticks for history\n      })\n\n      requestRef.current = requestAnimationFrame(animate)\n    }\n\n    requestRef.current = requestAnimationFrame(animate)\n\n    return () => {\n      window.removeEventListener(\
<truncated 2644 bytes>