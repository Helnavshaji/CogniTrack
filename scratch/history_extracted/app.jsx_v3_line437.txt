import { useState, useEffect } from 'react'\nimport { BrowserRouter as Router, Routes, Route, useLocation } from 'react-router-dom'\nimport { AnimatePresence, motion } from 'framer-motion'\nimport Lenis from 'lenis'\nimport ForestScene from './components/ForestScene'\nimport FloatingNav from './components/FloatingNav'\nimport SideDashboard from './components/SideDashboard'\nimport CustomCursor from './components/CustomCursor'\nimport Splash from './pages/Splash'\nimport Checkin from './pages/Checkin'\nimport Dashboard from './Dashboard'\nimport Lumie from './components/Lumie'\n\nfunction MainLayout({ userId }) {\n  const location = useLocation()\n  const [sideOpen, setSideOpen] = useState(false)\n  const [splashCompleted, setSplashCompleted] = useState(false)\n  const [mousePos, setMousePos] = useState({ x: 0, y: 0 })\n  const [activeCheckinState, setActiveCheckinState] = useState(\"idle\")\n  const [activeCheckinTone, setActiveCheckinTone] = useState(\"\")\n  const [forestZoom, setForestZoom] = useState(false)\n\n  // 1. Initialize Lenis Smooth Scroll\n  useEffect(() => {\n    const lenis = new Lenis({\n      duration: 1.5,\n      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)), // Natural deceleration\n      smoothWheel: true,\n    })\n\n    function raf(time) {\n      lenis.raf(time)\n      requestAnimationFrame(raf)\n    }\n\n    requestAnimationFrame(raf)\n\n    return () => {\n      lenis.destroy()\n    }\n  }, [])\n\n  // 2. Track normalized mouse coordinates (-1 to 1) for parallax\n  useEffect(() => {\n    const handleMouseMove = (e) => {\n      const x = (e.clientX - window.innerWidth / 2) / (window.innerWidth / 2)\n      const y = (e.clientY - window.innerHeight / 2) / (window.innerHeight / 2)\n      setMousePos({ x, y })\n    }\n    window.addEventListener(\"mousemove\", handleMouseMove)\n    return () => window.removeEventListener(\"mousemove\", handleMouseMove)\n  }, [])\n\n  // 3. Spin Lumie on route change\n  useEffect(() => {\n    setActiveCheckinState(\"done\")\n    const timer = setTim
<truncated 4182 bytes>